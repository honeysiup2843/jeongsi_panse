# CLAUDE.md

이 파일은 Claude(및 다른 AI 에이전트)가 이 저장소에서 작업할 때 따라야 할 지침이다.
사람이 읽어도 되지만, 1차 독자는 에이전트다.

---

## 1. 이 프로젝트가 하는 일

수험생이 수능 성적(국어·수학·영어·탐구)을 입력하면, **대학·학과마다 다른 영역별 반영비율과
영어 환산방식을 적용해 환산총점을 다시 계산하고**, 최근 합격선과의 격차로 약 1,500개 모집단위를
**위험 / 상향 / 소신 / 적정 / 하향** 다섯 칸에 배치하는 단일 페이지 웹앱.

핵심 주장은 하나다: **같은 성적이라도 대학마다 유리·불리가 갈린다.**
이 주장을 눈에 보이게 만드는 것이 이 앱의 존재 이유이므로,
"평균 백분위로 대충 줄 세우기"로 퇴행시키는 변경은 받지 않는다.

**산출물**: Artifact로 게시되는 단일 HTML 페이지. 서버·빌드 런타임·외부 API 없음.

---

## 2. 디렉터리

```
jeongsi-panse/
├── CLAUDE.md              이 파일 (에이전트 지침)
├── README.md              사람용 개요
├── package.json           npm test / npm run build / npm run check
├── src/
│   ├── data.js            반영비율 프로필                (전역 PROFILES) — 손으로 관리
│   ├── univs.js           대학·모집단위·학년도별 합격선  (전역 UNIVS) — 자동 생성, 직접 수정 금지
│   ├── engine.js          환산점수·5단계 판정 엔진     (전역 함수들)
│   └── app.html           UI. /*__DATA__*/ 와 /*__ENGINE__*/ 자리를 빌드가 채운다
├── scripts/
│   ├── build.js           src/* → dist/index.html (+ dist/preview.html)
│   ├── fetch-adiga.js     「어디가」 정시 결과 내려받기 → data/adiga/raw
│   └── import-adiga.js    data/adiga/raw → src/univs.js
├── data/adiga/raw/        「어디가」 원자료 (대학코드_학년도.json, 35개 대학 × 2023~2026)
├── tests/
│   ├── _load.js           빌드와 같은 순서로 data+engine을 합쳐 평가하는 로더
│   ├── harness.js         의존성 없는 최소 assert 하네스
│   ├── data.test.js       데이터 불변식 11종
│   └── engine.test.js     엔진 검증 40종 (회귀 테스트 5건 포함)
└── dist/
    ├── index.html         게시본 — Artifact에 올리는 파일
    └── preview.html       로컬 확인용 (doctype 래퍼가 붙은 버전)
```

**빌드는 문자열 치환 한 번이 전부다.** 번들러·트랜스파일러·프레임워크를 도입하지 말 것.
`npm install` 없이 `node`만 있으면 테스트와 빌드가 돌아가야 한다. (Playwright는 선택적 확인용.)

---

## 3. 명령

```bash
npm test      # 데이터 불변식 + 엔진 검증 (51개)
npm run build # dist/index.html 생성 + 게시 규칙 검증
npm run check # 위 둘을 순서대로

# 합격선 데이터 갱신 (네트워크 필요, 프록시 뒤에서는 NODE_USE_ENV_PROXY=1)
node scripts/fetch-adiga.js   # 새 파일만 받는다. 다시 받으려면 data/adiga/raw 해당 파일 삭제
node scripts/import-adiga.js  # src/univs.js 재생성
```

`npm run build`는 통과 조건을 스스로 검사한다: 금지 태그(`<!doctype>` / `<html>` / `<head>` /
`<body>`) 없음, `<title>` 존재, 치환 안 된 플레이스홀더 없음, 허용 외 외부 호스트 없음.
하나라도 어기면 exit 1이며 `dist/`를 쓰지 않는다.

---

## 4. 데이터 규칙 (`src/data.js`)

### PROFILES — 반영비율 프로필

| 필드 | 의미 |
|---|---|
| `ratio` | `[국어, 수학, 영어, 탐구]` 퍼센트. **합이 반드시 100.** |
| `base` | `'std'` 표준점수 반영 / `'pct'` 백분위 반영 |
| `engMode` | `'ratio'` 영어가 반영비율 안에 포함 / `'penalty'` 총점에서 가·감점 |
| `engT` | 1~9등급 값 **9개**. `ratio`형은 100점 만점 환산점, `penalty`형은 1000점 만점 기준 가·감점 |
| `mbonus` | 미적분·기하 선택 시 수학 취득점수 가산율(%) |
| `sbonus` | 과학탐구 선택 시 탐구 취득점수 가산율(%) |
| `conf` | `'c'` 공개자료로 확인 / `'e'` 추정치 |

**불변식 (tests/data.test.js가 강제):**
- `ratio` 합계 = 100 (±0.05), 길이 4, 음수 없음
- `engMode === 'ratio'` ⟹ `ratio[2] > 0`
- `engMode === 'penalty'` ⟹ `ratio[2] === 0` ← 이걸 어기면 영어 배점이 **조용히 증발한다**
- `engT`는 등급이 낮아질수록 값이 오르지 않음 (단조 비증가)

### UNIVS — 대학·모집단위 (`src/univs.js`, 자동 생성)

**직접 고치지 않는다.** 원자료는 대교협 대입정보포털 「어디가」의 정시(수능위주) 전형 결과이고,
`scripts/import-adiga.js`가 규칙대로 변환한다. 결과가 이상하면 import 규칙을 고치고 재생성한다.

| 필드 | 의미 |
|---|---|
| `n` | 모집단위명 — 「어디가」 최신 학년도 표기 그대로 |
| `p` | 참조할 PROFILES 키 (대학별 `_h`/`_s`, 의약 `med_*`, 교대 `edu_p`) |
| `g` | `'인문' \| '자연' \| '의약'` — 「어디가」에 없어 **모집단위명으로 분류** (`classify()`) |
| `mg` | 모집군 `'가' \| '나' \| '다'` (최신 학년도) |
| `cut` | 최신 학년도 합격선. **최종등록자 70% 컷의 국·수·탐 평균백분위** |
| `cy` | `cut`의 학년도 |
| `conf` | `'c'` 「어디가」 평균백분위 그대로 / `'e'` 평균백분위 미공개로 과목별 70% 컷에서 계산 |
| `cuts` / `cconf` | 학년도별(2023~2026) 합격선과 그 conf. 결과가 있는 해만 |
| `eng` | 학년도별 70% 컷 수험생의 영어 등급 |
| `rate` / `final` | 최신 학년도 경쟁률 / 최종 모집인원 |

import 규칙 요약 (자세한 건 스크립트 머리 주석):
- 특별전형(농어촌·기회균형·특성화·실기·지역인재 등)은 제외, 일반전형만.
- 예체능 모집단위는 제외 (앱 범위 밖).
- 최근 2개 학년도(2025·2026)에 결과가 없는 모집단위는 폐지·개편으로 보고 제외.
- 이전 학년도는 **모집단위명이 같을 때만** 이어 붙인다. 이름이 바뀐 단위를 추측으로 잇지 않는다.
- 가천대는 「어디가」에 환산점수만 공개해 백분위 합격선이 없다 → 모집단위 0개.

학과 키는 `대학id·모집단위명`으로 만들어진다(예: `sog·경영학부(경영학전공)`). 이 키는 사용자의
localStorage(반영비율 덮어쓰기, 담아둔 원서 3장)에 저장된다. 「어디가」가 이름을 바꾸면 키도 바뀌어
저장된 값이 조용히 풀린다 — 데이터 갱신 커밋 메시지에 이름이 바뀐 단위 수를 남긴다.

### 데이터를 다룰 때 지킬 것

1. **출처 없는 숫자를 `conf: 'c'`로 올리지 않는다.** 확인 못 했으면 `'e'`다. 합격선 `conf`와
   프로필 `conf`(반영비율) 중 **하나라도** `'e'`이면 UI에 `추정` 배지가 붙는다(`[E11]`) — 사용자가 무엇을 믿을지 판단할 근거를 뺏지 말 것.
2. **모르면 지어내지 말고 비워두거나 물어본다.** 이 프로젝트에서 가장 위험한 실패는 크래시가
   아니라 **그럴듯한 가짜 숫자**다. 입시 결정에 쓰이는 도구다.
3. 반영비율을 추가·수정하면 반드시 `npm test`를 돌린다. 합계 100이 깨지는 실수가 가장 흔하다.
4. 합격선은 연도별 변동이 크다. 합격선은 학년도별로 `cuts`에 따로 보관하고, 섞어서 한 값으로 만들지 않는다.
5. 반영비율 프로필(PROFILES)은 여전히 대학별 인문/자연 2종이다. 모집단위가 늘어도 **학과별 반영비율을
   새로 지어내지 않는다.** 모집요강으로 확인한 경우에만 프로필을 추가하고 `conf:'c'`로 둔다.

---

## 5. 엔진 규칙 (`src/engine.js`)

### 계산 순서

```
성적 입력
  ↓ ① 영역 정규화 — base='std'이면 취득표준점수÷만점표준점수, base='pct'이면 백분위
  ↓ ② 대응표 보정 — 아래 참조
  ↓ ③ 환산총점 = Σ(정규화값 × 반영비율) ÷ 10   (1000점 만점)
  ↓    + 영어 (ratio형은 비율 안에서, penalty형은 총점에 가·감점)
  ↓    + 미적분·기하 / 과탐 가산
  ↓ ④ 격차 = (내 환산총점 − 합격선 환산총점) ÷ 10   [단위: %p]
  ↓ ⑤ 5단계 분류 + 로지스틱 합격확률
```

합격선 환산총점은 **국·수·탐이 모두 해당 백분위이고, 영어는 그 학과 70% 컷 수험생의 실측 등급인
가상 수험생**으로 계산한다(`cutEngOf()`). 「어디가」는 영어 등급을 2026학년도에만 공개하므로 두 합격선
기준 모두 그 값을 쓴다. 실측이 없는 학과(7개)만 1등급으로 가정하고 `cutEngAssumed`로 UI에 표시한다.
예전처럼 전 학과를 1등급으로 가정하면 영어 2등급 이하 학생이 체계적으로 불리해진다 — `[E14]`.
가산점은 적용하지 않는다(합격선이 평균 백분위 기준이므로).

### ★ 대응표 보정 — 이 프로젝트에서 가장 중요한 한 줄

합격선은 **백분위**로 저장돼 있는데, 표준점수 반영 대학에서는 이걸 표준점수로 바꿔야 한다.
그런데 수험생이 입력한 (표준점수, 백분위) 쌍이 내장 대응표와 어긋나면 —
실제 성적표에서 흔히 일어난다 — **같은 학생이 표준점수형 대학에서는 백분위 99처럼,
백분위형 대학에서는 96처럼 취급된다.**

그래서 `calibrate()`가 **수험생 본인의 성적표가 지나는 점을 지나도록 대응 곡선을
평행이동**시킨다. 이 보정이 없으면 백분위 95 학생이 199곳 중 182곳에서 "안정"으로 나왔다(학과 199개 시절 측정).
현재 데이터에서 보정을 끄면 관심권 학과의 두 방식 격차가 평균 0.6%p → 5.0%p로 벌어진다.

→ **이 보정을 제거하거나 우회하는 변경은 금지.** `tests/engine.test.js [E6]`이 회귀를 막고 있다.

### 엔진을 수정할 때 지킬 것

1. `analyze(me, th, overrides, cutMode)`의 반환 형태(`{key, u, d, g, mg, cutP, cutLabel, cuts, cconf, eng70,
   rate, final, cutEng, cutEngYear, cutEngAssumed, conf, cutConf, ratioConf, prof, score, parts, caps, cutTotal, cutParts, diff, tier, p}`)는
   UI와의 계약이다. `conf`는 `cutConf`/`ratioConf`를 합친 값이다. 필드를 빼면 UI가 조용히 깨진다.
   `cutMode`는 `'latest'`(기본, 최신 학년도) / `'avg'`(결과 있는 학년도 평균) — `cutOf()`, `[E13]`.
2. **단조성을 깨지 말 것**: 성적이 오르면 어떤 학과에서도 격차가 줄면 안 된다.
   영어 등급이 나빠지면 어떤 학과에서도 격차가 늘면 안 된다. (`[E3]`, `[E5]`)
3. 5단계 경계값(`DEF_TH`)은 사용자가 슬라이더로 바꾼다. 하드코딩된 경계를 새로 만들지 말 것.
4. 결측 입력(전부 0)에서도 예외 없이 돌고 NaN이 새지 않아야 한다. (`[E10]`)
   탐구는 `tamAvg()`로 **입력된 과목만** 평균한다. 빈 과목을 0점으로 평균하지 말 것. (`[E12]`)
5. `engine.js`는 전역 `PROFILES`/`UNIVS`에 의존한다. `module.exports`나 `import`를 넣지 말 것 —
   그 순간 브라우저에서 깨진다. 테스트는 `tests/_load.js`가 빌드와 같은 방식으로 합쳐 쓴다.

---

## 6. UI 규칙 (`src/app.html`)

### 시각 언어

- **글래스모피즘.** 배경에 5단계 색으로 물든 오로라 블롭 + `backdrop-filter` 유리 패널.
  블롭 위에는 `--scrim`이 깔려 가독성을 지킨다. **블롭 불투명도를 올리지 말 것** —
  초안에서 성적 입력 패널이 읽히지 않아 낮춘 값이다.
- **색은 토큰으로만.** 모든 색은 `:root`에 정의되고, 다크는 `@media (prefers-color-scheme:dark)
  :root:not([data-theme="light"])` 와 `:root[data-theme="dark"]` **두 곳 모두**에 다시 정의한다.
  한쪽에만 넣으면 테마 전환이 한 방향으로만 동작한다.
- **5단계 팔레트는 검증된 값이다.** 위험 `#cc2f4e` / 상향 `#d17b18` / 소신 `#7d5fd6` /
  적정 `#0d9c68` / 하향 `#2a70c8` (라이트), 다크는 대응하는 별도 스텝.
  색맹 인접쌍 분리(ΔE)와 명도 대역 검사를 통과한 조합이므로 **임의로 바꾸지 말 것.**
  바꿔야 하면 dataviz 스킬의 `validate_palette.js`로 재검증한다.
  색은 단독으로 의미를 나르지 않는다 — 항상 단계 이름 텍스트가 함께 붙는다.
- **글꼴**: 본문·UI `IBM Plex Sans KR`, 숫자 `IBM Plex Mono`(tabular-nums), 표제 `Noto Serif KR`.
  Google Fonts 외 폰트 호스트는 CSP에 막힌다.
- **한글 줄바꿈**: 전역으로 `word-break: keep-all`. 지우면 "다시 계/산하고"처럼 어절이 쪼개진다.

### 동작

- 페이지는 **예시 성적이 채워진 상태로 열린다.** 빈 폼으로 열지 말 것 — 무엇을 하는 앱인지
  첫 화면에서 보여야 한다. 예시임은 명시한다.
- localStorage는 **모두 try/catch**로 감싼다(`jp.scores`, `jp.th`, `jp.ov`, `jp.picks`, `jp.cutmode`).
  실패해도 페이지는 정상 동작해야 한다.
- 반영비율 직접 수정: 합계 100이어야 적용. **감점형 대학의 영어 칸은 disabled** —
  풀면 영어 배점이 증발한다(`[E9]`가 잡아낸 결함).
- 원서 3장 트레이는 가·나·다군 각 1장. 정시 지원 규칙 그대로다.

### 하지 말 것

- 외부 스크립트·스타일시트 추가 (CSP: Google Fonts만 허용). 라이브러리 없이 만들어져 있다.
- `<!doctype>` / `<html>` / `<head>` / `<body>` 태그 추가 — Artifact가 골격을 붙인다.
- 면책 배너와 「계산 방법」 푸터 삭제. 이 도구가 무엇을 근사하고 있는지 밝히는 부분이다.

---

## 7. 변경 절차

1. `src/` 아래를 고친다. `dist/`는 **절대 직접 수정하지 않는다** (빌드 산출물).
2. `npm run check` — 테스트 51개 통과 + 빌드 성공.
3. 시각 변경이면 `dist/preview.html`을 브라우저(또는 Playwright)로 **한 번** 확인한다.
   라이트/다크/모바일(400px) 세 폭에서 가로 스크롤이 생기지 않아야 한다.
4. `dist/index.html`을 기존 Artifact URL에 **재게시**한다 (새 아티팩트를 만들지 않는다).
5. 계산 규칙·데이터 규칙을 바꿨으면 이 파일(CLAUDE.md)의 해당 절도 같이 고친다.

### 새 동작을 추가할 때

버그를 고쳤으면 **그 버그를 재현하는 테스트를 먼저 남긴다.** `[E2]`, `[E6]`, `[E14]`가 그 예다.
둘 다 실제로 겪은 오류이고, 테스트 이름에 "회귀"라고 적혀 있다.

---

## 8. 알려진 한계 (사용자에게 숨기지 말 것)

- 합격선 1,539개는 모두 「어디가」 공식 자료지만, **반영비율은 1,539개 중 409개만 공개자료로 확인**됐다.
  나머지는 추정 프로필이고, 한 대학 안에서는 계열(인문/자연)별로 같은 반영비율을 쓴다.
- 여러 학년도 합격선이 있는 모집단위는 668개뿐이다. 2024·2025학년도는 평균백분위를 공개하지 않은
  대학이 많고(공개 기준 변경), 이름이 바뀐 모집단위는 잇지 않았다.
- 70% 컷 백분위는 **70번째 등록자의 백분위**라서 과목별 값이 고르지 않고, 정수로 공개된다.
- 계열(인문/자연) 분류는 모집단위명 기반 규칙이라 틀릴 수 있다. 틀리면 다른 계열의 반영비율이 적용된다.
- 가천대는 「어디가」에 백분위 합격선이 없어 목록에서 빠졌다.
- 표준↔백분위 대응표는 실제 수능 분포의 **근사**다. 보정을 거쳐도 완전하지 않다.
- 합격확률은 격차를 로지스틱에 넣은 **참고 수치**다. 경쟁률, 추가합격 이월, 군별 연쇄이동,
  수능 최저학력기준, 학생부 반영을 계산하지 않는다.
- 예체능·특수전형·지역인재·논술은 범위 밖이다.

> 이 도구는 진학 상담을 대체하지 않는다. 판단의 출발점이지 결론이 아니다.
> 이 문장을 UI에서 지우자는 요청은 거절한다.
